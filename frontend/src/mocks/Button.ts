import { ButtonTypes } from "components/Button/Button.types";

export const mock_Button: ButtonTypes = {
  children: "Reserve Ticket",
  model: "full",
  onCLickButton: () => console.log("Click in the Button"),
};
